name: flexi_formatter
version: 1.5.0
description: A flexible and customizable Dart/Flutter library for formatting numbers, dates, and other data types.
homepage: https://github.com/FlexiKline
repository: https://github.com/FlexiKline/flexi_formatter

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  decimal: ^2.3.3

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
